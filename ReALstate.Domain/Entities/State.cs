namespace ReALstate.Domain.Entities
{
    // The State enum is responsible for defining the possible states of an estate.
    public enum State
    {
        New,
        Great,
        Bad,
        RenovationNeeded
    }
}
